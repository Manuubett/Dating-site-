<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Bett Hub — Connect, Share & Grow Together</title>
  <meta name="description" content="Bett Hub — a friendly social community to share lifestyle posts, follow people, and discover ideas. Join the hub to connect with others." />
  <link rel="canonical" href="https://bett.website/" />
  <meta name="theme-color" content="#EF4444" />
<link rel="sitemap" type="application/xml" title="Sitemap" href="https://bett.website/sitemap.xml" />
  <!-- Defer non-critical scripts -->
  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-4347535849525164"
     crossorigin="anonymous"></script>
  <script async custom-element="amp-auto-ads"
        src="https://cdn.ampproject.org/v0/amp-auto-ads-0.1.js">
  </script>

  <!-- Open Graph -->
  <meta property="og:site_name" content="Bett Hub" />
  <meta property="og:title" content="Bett Hub — Connect, Share & Grow Together" />
  <meta property="og:description" content="Join the Bett Hub community — share posts, follow members, and discover lifestyle ideas." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://bett.website/" />
  <meta property="og:image" content="https://bett.website/assets/og-image.png" />

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Bett Hub — Connect, Share & Grow Together" />
  <meta name="twitter:description" content="Join the Bett Hub community — share posts, follow members, and discover lifestyle ideas." />
  <meta name="twitter:image" content="https://bett.website/assets/og-image.png" />

  <!-- Preconnect to critical domains -->
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="preconnect" href="https://firestore.googleapis.com" />
  
  <!-- Preload critical resources -->
  <link rel="preload" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" as="style" onload="this.onload=null;this.rel='stylesheet'">
  <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"></noscript>

  <!-- Critical CSS - Inlined and optimized -->
  <style>
    /* Critical above-the-fold styles */
    :root {
      --accent: #ef4444;
      --accent-hover: #dc2626;
      --accent-light: #fecaca;
      --muted: #6b7280;
      --light-muted: #9ca3af;
      --bg: #ffffff;
      --card-bg: #f9fafb;
      --max-w: 1200px;
      --radius: 16px;
      --gap: 20px;
      --shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.05), 0 4px 10px -2px rgba(0, 0, 0, 0.05);
      --shadow-lg: 0 20px 40px -10px rgba(0, 0, 0, 0.1);
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    }
    
    html, body {
      height: 100%;
      margin: 0;
      background: var(--bg);
      color: #111827;
      scroll-behavior: smooth;
    }
    
    .container {
      max-width: var(--max-w);
      margin: 0 auto;
      padding: 0 20px;
    }
    
    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      padding: 16px 0;
      position: sticky;
      top: 0;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      z-index: 1000;
      transition: all 0.3s ease;
    }
    
    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 700;
      transition: transform 0.2s ease;
    }
    
    .logo:hover {
      transform: translateY(-2px);
    }
    
    .logo .mark {
      width: 44px;
      height: 44px;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--accent), #f97316);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-size: 20px;
      font-weight: 700;
      box-shadow: 0 4px 10px rgba(239, 68, 68, 0.3);
    }
    
    nav {
      display: flex;
      gap: 16px;
      align-items: center;
    }
    
    a {
      color: inherit;
      text-decoration: none;
      transition: all 0.2s ease;
    }
    
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 20px;
      border-radius: 12px;
      border: 1px solid transparent;
      background: var(--accent);
      color: #fff;
      font-weight: 600;
      transition: all 0.3s ease;
      cursor: pointer;
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.2);
    }
    
    .btn:hover {
      background: var(--accent-hover);
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(239, 68, 68, 0.3);
    }
    
    .btn.ghost {
      background: transparent;
      color: var(--accent);
      border-color: rgba(239, 68, 68, 0.2);
      box-shadow: none;
    }
    
    .btn.ghost:hover {
      background: rgba(239, 68, 68, 0.1);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.1);
    }
    
    .hero {
      display: grid;
      grid-template-columns: 1fr;
      gap: 40px;
      padding: 60px 0;
      align-items: center;
    }
    
    .hero .title {
      font-size: 36px;
      line-height: 1.1;
      margin: 0;
      font-weight: 700;
      background: linear-gradient(135deg, #111827, #374151);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    
    .hero p {
      color: var(--muted);
      margin: 16px 0 0;
      font-size: 18px;
      line-height: 1.6;
    }
    
    /* Non-critical CSS will be loaded later */
  </style>

  <!-- Load non-critical CSS asynchronously -->
  <link rel="preload" href="/css/non-critical.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
  <noscript><link rel="stylesheet" href="/css/non-critical.css"></noscript>

  <!-- Structured Data -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "Bett Hub",
    "url": "https://bett.website/",
    "potentialAction": {
      "@type": "SearchAction",
      "target": "https://bett.website/search?q={search_term_string}",
      "query-input": "required name=search_term_string"
    }
  }
  </script>
</head>
<body>
  <div class="container" role="main">
    <header>
      <div class="logo" aria-label="Bett Hub">
        <span class="mark" aria-hidden="true">BH</span>
        <div>
          <div style="font-size:16px">Bett Hub</div>
          <div style="font-size:12px;color:var(--muted);margin-top:2px">Connect • Share • Grow</div>
        </div>
      </div>
      
      <button class="mobile-menu-btn" id="mobileMenuBtn" aria-label="Toggle menu">☰</button>
      
      <nav aria-label="Main Navigation" id="mainNav">
        <a href="home2.html" class="nav-link">Explore</a>
        <a href="about.html" class="nav-link">About</a>
        <a href="contact1.html" class="nav-link">Contact</a>
        <a href="login.html" class="btn" style="margin-left:10px">Login</a>
        <a href="login.html" class="btn ghost" style="margin-left:8px">Sign up</a>
      </nav>
    </header>

    <!-- Hero Section - LCP Candidate -->
    <section class="hero" aria-labelledby="home-heading">
      <div class="fade-in">
        <h1 id="home-heading" class="title">Welcome to Bett Hub — Build real connections</h1>
        <p>Share stories, discover local lifestyle posts, and follow interesting people. Public previews below so visitors can see quality content without logging in.</p>

        <div style="margin-top:30px;display:flex;gap:12px;flex-wrap:wrap">
          <a class="btn" href="login.html">Create an account</a>
          <a class="btn ghost" href="home2.html">Explore public posts</a>
        </div>

        <!-- Stats -->
        <div class="stats fade-in delay-1">
          <div class="stat">
            <span class="stat-number" id="userCount">1.2K</span>
            <span class="stat-label">Active Users</span>
          </div>
          <div class="stat">
            <span class="stat-number" id="postCount">5.7K</span>
            <span class="stat-label">Community Posts</span>
          </div>
          <div class="stat">
            <span class="stat-number" id="communityCount">24</span>
            <span class="stat-label">Local Communities</span>
          </div>
        </div>
      </div>

      <aside class="card fade-in delay-2" style="text-align:center;overflow:hidden">
        <!-- Optimized image with explicit dimensions -->
        <img 
          src="https://i.ibb.co/Y4bz3tHf/web-app-manifest-192x192.png" 
          alt="Bett Hub preview" 
          width="192" 
          height="192"
          style="width:100%;height:auto;border-radius:8px;display:block" 
          loading="eager" 
          decoding="async" />
        <div class="meta" style="margin-top:10px">Public feed preview (visible to everyone)</div>
      </aside>
    </section>

    <!-- Features -->
    <section aria-label="Features" style="margin-top:60px">
      <h2 style="font-size:28px;margin:0 0 20px 0;text-align:center">Why join Bett Hub?</h2>
      
      <div class="features">
        <div class="feature card fade-in">
          <div style="width:40px;height:40px;border-radius:10px;background:var(--accent-light);display:flex;align-items:center;justify-content:center;margin-bottom:16px">
            <span style="color:var(--accent);font-size:20px">📝</span>
          </div>
          <strong>Public Posts</strong>
          <div class="meta" style="margin-top:8px">Read community posts without signing in — shows good content to reviewers.</div>
        </div>
        <div class="feature card fade-in delay-1">
          <div style="width:40px;height:40px;border-radius:10px;background:var(--accent-light);display:flex;align-items:center;justify-content:center;margin-bottom:16px">
            <span style="color:var(--accent);font-size:20px">👤</span>
          </div>
          <strong>Profiles</strong>
          <div class="meta" style="margin-top:8px">User pages with bios, recent posts, and follower counts.</div>
        </div>
        <div class="feature card fade-in delay-2">
          <div style="width:40px;height:40px;border-radius:10px;background:var(--accent-light);display:flex;align-items:center;justify-content:center;margin-bottom:16px">
            <span style="color:var(--accent);font-size:20px">🔒</span>
          </div>
          <strong>Privacy-first</strong>
          <div class="meta" style="margin-top:8px">GDPR-friendly privacy controls and clear terms.</div>
        </div>
      </div>
    </section>

    <!-- Live Public Posts -->
    <section aria-label="Latest public posts" style="margin-top:60px">
      <h2 style="font-size:28px;margin:0 0 20px 0">Latest from the community</h2>

      <div class="posts" id="publicPosts">
        <div class="loading">Loading real posts from the community...</div>
      </div>

      <div style="margin-top:20px;text-align:center">
        <a href="home2.html" class="btn ghost">See more public posts →</a>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta fade-in">
      <h2>Ready to join our community?</h2>
      <p>Create your account today and start sharing your stories, connecting with others, and discovering new ideas.</p>
      <a class="btn" href="login.html">Get Started Now</a>
    </section>

    <!-- Footer -->
    <footer>
      <div style="display:flex;justify-content:space-between;flex-wrap:wrap;gap:20px;align-items:center">
        <div>
          <strong>Bett Hub</strong> — <span class="meta">A friendly community for sharing lifestyle ideas.</span>
        </div>
        <div style="display:flex;gap:16px;align-items:center">
          <a href="privacy.html" class="nav-link">Privacy</a>
          <a href="terms.html" class="nav-link">Terms</a>
          <a href="/sitemap.xml" class="nav-link">Sitemap</a>
          <a href="contact1.html" class="nav-link">Contact</a>
        </div>
      </div>
      <div style="margin-top:16px;color:var(--muted);text-align:center">© <span id="year"></span> Bett Hub. All rights reserved.</div>
    </footer>
  </div>

  <!-- Floating Action Button -->
  <div class="fab" id="fab" aria-label="Create new post" title="Create new post">
    +
  </div>

  <!-- Deferred JavaScript -->
  <script>
    // Set year immediately
    document.getElementById('year').textContent = new Date().getFullYear();
    
    // Load non-critical resources after page is interactive
    if ('requestIdleCallback' in window) {
      window.requestIdleCallback(loadDeferredResources);
    } else {
      window.addEventListener('load', loadDeferredResources);
    }
    
    function loadDeferredResources() {
      // Load Firebase SDKs
      const firebaseScript = document.createElement('script');
      firebaseScript.src = 'https://www.gstatic.com/firebasejs/8.10.0/firebase-app.js';
      firebaseScript.async = true;
      document.head.appendChild(firebaseScript);
      
      firebaseScript.onload = function() {
        const firestoreScript = document.createElement('script');
        firestoreScript.src = 'https://www.gstatic.com/firebasejs/8.10.0/firebase-firestore.js';
        firestoreScript.async = true;
        document.head.appendChild(firestoreScript);
        
        firestoreScript.onload = function() {
          // Initialize Firebase and load posts
          initializeFirebase();
        };
      };
      
      // Load AMP auto ads
      const ampScript = document.createElement('script');
      ampScript.async = true;
      ampScript.src = 'https://cdn.ampproject.org/v0/amp-auto-ads-0.1.js';
      document.head.appendChild(ampScript);
    }
    
    function initializeFirebase() {
      const firebaseConfig = {
        apiKey: "AIzaSyD9dwHPDTJr1-mFXPW_JZnVS-JyZ6BNMgU",
        authDomain: "smart-waste-66279.firebaseapp.com",
        projectId: "smart-waste-66279",
        storageBucket: "smart-waste-66279.appspot.com",
        messagingSenderId: "619099585646",
        appId: "1:619099585646:web:b193d2a9fc13347d88ba7f"
      };
      
      if (typeof firebase !== 'undefined') {
        firebase.initializeApp(firebaseConfig);
        const db = firebase.firestore();
        
        // Load posts after Firebase is ready
        loadPublicPosts(db);
        setupEventListeners();
      }
    }
    
    function setupEventListeners() {
      // Mobile menu toggle
      const mobileMenuBtn = document.getElementById('mobileMenuBtn');
      const mainNav = document.getElementById('mainNav');
      
      if (mobileMenuBtn && mainNav) {
        mobileMenuBtn.addEventListener('click', function(e) {
          e.stopPropagation();
          mainNav.classList.toggle('open');
        });

        // Close mobile menu when clicking outside
        document.addEventListener('click', function(event) {
          if (!mainNav.contains(event.target) && !mobileMenuBtn.contains(event.target)) {
            mainNav.classList.remove('open');
          }
        });
      }

      // FAB click handler
      const fab = document.getElementById('fab');
      if (fab) {
        fab.addEventListener('click', function() {
          window.location.href = 'login.html';
        });
      }
    }
    
    function loadPublicPosts(db) {
      const postsContainer = document.getElementById('publicPosts');
      if (!postsContainer) return;
      
      db.collection('posts')
        .orderBy('createdAt', 'desc')
        .limit(6)
        .onSnapshot(async (snapshot) => {
          if (snapshot.empty) {
            postsContainer.innerHTML = '<div class="no-posts">No posts yet. Be the first to share!</div>';
            return;
          }

          postsContainer.innerHTML = ''; // Clear loading

          for (const doc of snapshot.docs) {
            const p = doc.data();
            const postId = doc.id;

            // Fetch author
            let authorName = 'Anonymous';
            let authorPhoto = 'https://via.placeholder.com/84?text=👤';
            if (p.uid) {
              const userSnap = await db.collection('users').doc(p.uid).get();
              if (userSnap.exists) {
                const u = userSnap.data();
                authorName = u.fullName || 'Anonymous';
                authorPhoto = u.photoURL || authorPhoto;
              }
            }

            // Format date
            const date = p.createdAt ? p.createdAt.toDate() : new Date();
            const formattedDate = date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });

            // Get full text
            const text = p.text || '';
            
            // Media thumbnail
            let thumbHTML = `<div class="thumb" style="background:#f3f4f6;display:flex;align-items:center;justify-content:center;color:var(--muted);font-size:14px">No Image</div>`;
            if (p.mediaUrl) {
              thumbHTML = p.mediaType === 'video'
                ? `<video class="thumb" src="${p.mediaUrl}" loop muted preload="metadata" style="object-fit:cover" loading="lazy"></video>`
                : `<img class="thumb" src="${p.mediaUrl}" alt="Post image" loading="lazy" width="100" height="100">`;
            } else if (p.bgColor && p.text) {
              thumbHTML = `<div class="thumb" style="background:${p.bgColor};display:flex;align-items:center;justify-content:center;padding:8px;font-size:12px;color:#333;text-align:center">${text.substring(0, 50)}...</div>`;
            }

            // Create article
            const article = document.createElement('article');
            article.className = 'post card fade-in';
            article.setAttribute('itemprop', 'blogPost');
            article.setAttribute('itemscope', '');
            article.setAttribute('itemtype', 'https://schema.org/Article');

            article.innerHTML = `
              ${thumbHTML}
              <div style="flex:1">
                <div style="display:flex;justify-content:space-between;gap:12px;align-items:flex-start">
                  <div>
                    <h3 style="margin:0;font-size:18px;line-height:1.3" itemprop="headline">${text.substring(0, 60) || 'Untitled Post'}</h3>
                    <div class="meta" style="margin-top:8px" itemprop="author" itemscope itemtype="https://schema.org/Person">
                      <span itemprop="name">by ${authorName}</span> • <time datetime="${date.toISOString()}" itemprop="datePublished">${formattedDate}</time>
                    </div>
                  </div>
                  <div class="meta" style="white-space:nowrap">${Math.ceil((text.length || 100) / 200)} min read</div>
                </div>
                <div class="post-content" style="margin:12px 0 0;color:var(--muted);max-width:100%">
                  ${text || 'No content'}
                </div>
                <button class="read-more-btn">Read more... <span>↓</span></button>
              </div>
            `;

            postsContainer.appendChild(article);
            
            // Set up read more functionality for this post
            setupReadMore(article, text);
          }
        }, (error) => {
          console.error("Failed to load posts:", error);
          postsContainer.innerHTML = '<div class="no-posts">Failed to load posts. Please try again later.</div>';
        });
    }
    
    function setupReadMore(postElement, fullText) {
      const contentElement = postElement.querySelector('.post-content');
      const readMoreBtn = postElement.querySelector('.read-more-btn');
      
      if (fullText.length > 120) {
        contentElement.classList.add('collapsed');
        readMoreBtn.style.display = 'inline-flex';
        
        readMoreBtn.addEventListener('click', function() {
          if (contentElement.classList.contains('collapsed')) {
            contentElement.classList.remove('collapsed');
            readMoreBtn.innerHTML = 'Read less <span>↑</span>';
          } else {
            contentElement.classList.add('collapsed');
            readMoreBtn.innerHTML = 'Read more... <span>↓</span>';
          }
        });
      } else {
        readMoreBtn.style.display = 'none';
      }
    }
    
    // Animate stats counter when they come into view
    function animateValue(id, start, end, duration) {
      let obj = document.getElementById(id);
      if (!obj) return;
      
      let range = end - start;
      let current = start;
      let increment = end > start ? 1 : -1;
      let stepTime = Math.abs(Math.floor(duration / range));
      let timer = setInterval(function() {
        current += increment;
        obj.textContent = current;
        if (current == end) {
          clearInterval(timer);
        }
      }, stepTime);
    }

    // Start animation when stats are in viewport
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          animateValue('userCount', 0, 1200, 2000);
          animateValue('postCount', 0, 5700, 2000);
          animateValue('communityCount', 0, 24, 1500);
          observer.disconnect();
        }
      });
    });

    const statsElement = document.querySelector('.stats');
    if (statsElement) {
      observer.observe(statsElement);
    }
  </script>

  <!-- Non-critical CSS (would be in a separate file) -->
  <style id="non-critical-css">
    /* This would normally be in a separate CSS file */
    .card {
      background: #fff;
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 24px;
      transition: all 0.3s ease;
      border: 1px solid rgba(0, 0, 0, 0.03);
    }
    
    .card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-lg);
    }
    
    .features {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
      margin-top: 40px;
    }
    
    .feature {
      padding: 20px;
      border-radius: var(--radius);
      border: 1px solid rgba(15, 23, 42, 0.05);
      transition: all 0.3s ease;
    }
    
    .feature:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-lg);
    }
    
    .feature strong {
      font-size: 18px;
      display: block;
      margin-bottom: 8px;
    }
    
    .posts {
      display: grid;
      gap: 20px;
      margin-top: 30px;
    }
    
    .post {
      display: flex;
      gap: 20px;
      align-items: flex-start;
      transition: all 0.3s ease;
    }
    
    .post:hover {
      transform: translateX(5px);
    }
    
    .post .thumb {
      width: 100px;
      height: 100px;
      border-radius: 12px;
      flex: 0 0 100px;
      object-fit: cover;
      background: #f3f4f6;
      transition: all 0.3s ease;
    }
    
    .post:hover .thumb {
      transform: scale(1.05);
    }
    
    .meta {
      font-size: 14px;
      color: var(--muted);
    }
    
    .loading {
      text-align: center;
      color: var(--muted);
      font-style: italic;
      margin: 40px 0;
      padding: 20px;
    }
    
    .no-posts {
      text-align: center;
      color: var(--muted);
      padding: 40px 20px;
      background: var(--card-bg);
      border-radius: var(--radius);
    }
    
    footer {
      margin-top: 60px;
      padding: 30px 0;
      color: var(--muted);
      font-size: 14px;
      border-top: 1px solid rgba(15, 23, 42, 0.06);
    }
    
    /* Read More functionality */
    .post-content {
      position: relative;
      overflow: hidden;
      line-height: 1.6;
    }
    
    .post-content.collapsed {
      max-height: 80px;
    }
    
    .read-more-btn {
      color: var(--accent);
      background: none;
      border: none;
      cursor: pointer;
      font-weight: 600;
      padding: 4px 0;
      margin-top: 8px;
      font-size: 14px;
      display: inline-flex;
      align-items: center;
      gap: 4px;
    }
    
    .read-more-btn:hover {
      text-decoration: underline;
    }
    
    /* Enhanced Navigation */
    .nav-link {
      color: var(--muted);
      padding: 8px 16px;
      border-radius: 8px;
      transition: all 0.2s ease;
      font-weight: 500;
    }
    
    .nav-link:hover {
      color: var(--accent);
      background: rgba(239, 68, 68, 0.05);
    }
    
    /* Mobile Menu */
    .mobile-menu-btn {
      display: none;
      background: none;
      border: none;
      cursor: pointer;
      font-size: 24px;
      color: var(--muted);
      padding: 8px;
      border-radius: 8px;
      transition: all 0.2s ease;
    }
    
    .mobile-menu-btn:hover {
      background: rgba(0, 0, 0, 0.05);
    }
    
    /* Stats Section */
    .stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
      margin: 40px 0;
    }
    
    .stat {
      text-align: center;
      padding: 20px;
    }
    
    .stat-number {
      font-size: 36px;
      font-weight: 700;
      color: var(--accent);
      display: block;
      margin-bottom: 8px;
    }
    
    .stat-label {
      color: var(--muted);
      font-size: 14px;
    }
    
    /* CTA Section */
    .cta {
      background: linear-gradient(135deg, var(--accent), #f97316);
      color: white;
      padding: 60px 40px;
      border-radius: var(--radius);
      text-align: center;
      margin: 60px 0;
    }
    
    .cta h2 {
      margin: 0 0 16px;
      font-size: 32px;
    }
    
    .cta p {
      margin: 0 0 30px;
      opacity: 0.9;
      font-size: 18px;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }
    
    .cta .btn {
      background: white;
      color: var(--accent);
    }
    
    .cta .btn:hover {
      background: rgba(255, 255, 255, 0.9);
    }
    
    /* Floating Action Button */
    .fab {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background: var(--accent);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      box-shadow: 0 6px 20px rgba(239, 68, 68, 0.4);
      cursor: pointer;
      z-index: 100;
      transition: all 0.3s ease;
    }
    
    .fab:hover {
      transform: scale(1.1);
      box-shadow: 0 8px 25px rgba(239, 68, 68, 0.5);
    }
    
    @media (min-width: 768px) {
      .hero {
        grid-template-columns: 1fr 1fr;
      }
      
      .hero .title {
        font-size: 48px;
      }
      
      .features {
        grid-template-columns: repeat(3, 1fr);
      }
    }
    
    @media (max-width: 768px) {
      .mobile-menu-btn {
        display: block;
      }
      
      nav {
        display: none;
        position: absolute;
        top: 80px;
        right: 20px;
        background: white;
        box-shadow: var(--shadow-lg);
        border-radius: 12px;
        padding: 16px;
        flex-direction: column;
        width: 200px;
        z-index: 100;
      }
      
      nav.open {
        display: flex;
      }
      
      .features, .stats {
        grid-template-columns: 1fr;
      }
      
      .hero .title {
        font-size: 32px;
      }
      
      .post {
        flex-direction: column;
      }
      
      .post .thumb {
        width: 100%;
        height: 200px;
      }
    }
    
    /* Animation for page load */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .fade-in {
      animation: fadeInUp 0.6s ease forwards;
    }
    
    .delay-1 {
      animation-delay: 0.1s;
    }
    
    .delay-2 {
      animation-delay: 0.2s;
    }
    
    .delay-3 {
      animation-delay: 0.3s;
    }
  </style>
</body>
</html>
